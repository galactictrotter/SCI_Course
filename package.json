{
  "name": "sci-course-slides",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic HTML slide generator for Cours de SIC lessons.",
  "scripts": {
    "lesson": "ruby src/build_slides.rb",
    "validate": "ruby src/validate_slides.rb",
    "qa": "node src/browser_qa.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
